#!/bin/zsh

echo 'DROP DATABASE IF EXISTS `RGBplace`;' > assets/sql/rgbplace_database.sql
mysqldump  --routines --triggers --databases RGBplace -u root --password="als0emffp*scallop99" >> assets/sql/rgbplace_database.sql

echo "Back Up : `date +%Y%m%d\ %H:%M:%S`" >> shell/log
