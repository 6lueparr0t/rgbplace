#!/bin/zsh

echo 'DROP DATABASE IF EXISTS `home`;' > assets/sql/database.sql
mysqldump  --routines --triggers --databases home -u root --password="als0emffp*scallop99" >> assets/sql/database.sql

echo "Back Up : `date +%Y%m%d\ %H:%M:%S`" >> log/db
