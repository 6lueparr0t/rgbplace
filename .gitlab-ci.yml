# gitlab-ci
deploy-to-server:
  stage: deploy
  only:
    - master
  script:
    - cd /var/www/html
    - git add -A 
    - git stash
    - git pull --rebase origin master  
  when: always
  tags:
    - deploy
